[workspace]
members = [
    "crates/build-sandbox",
    "crates/cache",
    "crates/common",
    "crates/graph",
    "crates/minimal",
    "crates/spongebob",
]
resolver = "3"

package.edition = "2024"
package.publish = false

[profile.release]
strip = true
lto = true
codegen-units = 1

[workspace.dependencies]
anyhow = "1.0.100"
blake3 = "1"
bytes = "1.10.1" # keep in sync with reqwest
clap = { version = "4.5", features = ["derive"] }
codespan-reporting = { version = "0.12", features = [
    "serialization",
] } # Keep in sync with nickel-lang-core
dirs = "6"
docker_credential = "1.3"
flate2 = "1.1"
futures = "0.3"
generational-arena = "^0.2"
google-cloud-gax = "1.0.0"
google-cloud-storage = "1.0.0"
hakoniwa = { git = "https://github.com/souk4711/hakoniwa", branch = "main" }
hex = "0.4"
indoc = "^2.0"
nickel-lang-core = { version = "0.15.0", default-features = false, features = [
    "format",
] }
object = {version = "0.37", default-features = false, features = ["archive", "elf", "std", "read_core"]}
oci-client = { version = "0.15.0", features = [
    "rustls-tls",
], default-features = false }
oci-spec = "0.8.3"
path-absolutize = "^3.1"
rayon = "1.11"
regex = "1" # keep in sync with nickel-lang-core
reqwest = { version = "0.12", features = [
    "stream",
] } # keep in sync with google-cloud-gax
rustc-hash = "2.1.1"
serde = { version = "1.0", features = [
    "derive",
] } # Keep within the same minor version as nickel-lang-core
prost = "0.14.1"
serde_json = { version = "1.0" }
sha2 = "0.10.9"
shlex = "1.3"
size = "0.5"
smallvec = "1.15"
tar = "0.4"
tempfile = "3.23"
thiserror = "2.0"
tokio = { version = "1.47.1", features = ["full"] }
toml = "0.9"
tonic = "0.14.2"
tonic-prost = "0.14.2"
tonic-prost-build = "0.14.2"
tracing = "0.1"
tracing-indicatif = "0.3.13"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
url = "2.5" # keep in sync with reqwest
uuid = { version = "1.18", features = ["v4"] }
zstd = "0.13"

globset = "0.4"

# our crates
build-sandbox = { path = "crates/build-sandbox" }
graph = { version = "0.0.1", path = "crates/graph" }
cache = { version = "0.0.1", path = "crates/cache" }
common = { version = "0.0.1", path = "crates/common" }
