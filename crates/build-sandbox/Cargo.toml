[package]
name = "build-sandbox"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { workspace = true }
thiserror = "1.0"
glob = "0.3"
tracing = "0.1"
fs_extra = "1.3"

[target.'cfg(target_os = "linux")'.dependencies]
hakoniwa = "1.2"
