[upstream]
dir = "../fakepkgs"

[harness]
use = "fake-stack"
build_packages = ["extra-build-pkg"]
runtime_packages = ["extra-runtime-pkg"]

[tasks.task-inherits-stack]
exec = "exec"
packages = ["uroot"]
