[defaults]
harness = "rust"

[upstream] # NOTE: This gets swapped out under test
dir = "../fakepkgs"

[tasks.task-smoketest]
packages = ["uroot"]
bash = """
echo 'env:' && env && echo "pwd: $(pwd)"
"""
