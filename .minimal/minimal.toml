[upstream] # Source of software & tooling
repo = "https://github.com/gominimal/pkgs"
branch = "main"
locked_commit = "69dea3b8d2a975f2db18ab71ad56bca2bacc9f10"

[harness]
use = "rust"

[defaults]
profile = "dev"
state_key = "dev"

[tasks.claude]
exec = "claude"

[tasks.test]
exec = "cargo test"

[tasks.shell]
exec = "bash -l"
packages = ["htop", "btop"]

[tasks.startup-time]
exec = "date"

[outputs.bash-img]
type = "oci-image"
packages = ["bash", "coreutils", "diffutils", "findutils", "tar"]
env_vars.test = "ello"
cmd = ["/usr/bin/bash", "-l"]

[tasks.vim]
profile = "demo"
state_key = ""
exec = "vim"

[tasks.btop]
profile = "demo"
state_key = ""
exec = "btop"

[tasks.demo-shell]
profile = "demo"
state_key = "demo"
exec = "bash -l"

[tasks.python]
profile = "demo"
state_key = "demo"
exec = "python3"


[tasks.env-smoketest]
inherit_cwd = true
bash = """
echo 'env:' && env && echo "pwd: $(pwd)"
"""
